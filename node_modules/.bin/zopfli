#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-zopfli/bin/zopfli" "$@"
  ret=$?
else 
  node  "$basedir/../node-zopfli/bin/zopfli" "$@"
  ret=$?
fi
exit $ret
